<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="com.chainreaction.title" parent="TextAppearance.AppCompat.Title">
        <item name="textAllCaps">false</item>
        <item name="android:textSize">@dimen/title_size</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="com.chainreaction.subtitle" parent="TextAppearance.AppCompat.Title">
        <item name="textAllCaps">false</item>
        <item name="android:textSize">@dimen/subtitle_size</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="com.chainreaction.formEditText" parent="TextAppearance.AppCompat.Body1">
        <item name="textAllCaps">false</item>
        <item name="android:textSize">@dimen/form_edit_text_size</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:padding">@dimen/form_edit_text_margin_padding</item>
    </style>

    <style name="com.chainreaction.button" parent="TextAppearance.AppCompat.Button">
        <item name="textAllCaps">false</item>
        <item name="android:textSize">@dimen/form_edit_text_size</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:padding">@dimen/form_edit_text_margin_padding</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
